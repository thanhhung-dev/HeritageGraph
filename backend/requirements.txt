fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
httpx>=0.27.0
mlx-lm>=0.20.0
mlx>=0.20.0
networkx>=3.0
# ĐÃ BỎ: ollama, graphrag.
# Knowledge graph ở backend/core/kg.py được xây HOÀN TOÀN DETERMINISTIC (khớp
# tên thực thể + mẫu "danh từ loại + tên riêng" của tiếng Việt), nên không cần
# LLM lúc index: không cần Ollama, không cần pipeline của graphrag. Đổi lại
# graph không bao giờ bịa entity, và build hết 0.14s thay vì 8-12 giờ.
