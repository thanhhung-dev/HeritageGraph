#cloud-config
# Cloud-init: tự động cài Docker + Docker Compose khi VM khởi động lần đầu
package_update: true
package_upgrade: false

packages:
  - ca-certificates
  - curl
  - gnupg
  - git

runcmd:
  # Cài Docker theo hướng dẫn chính thức cho Ubuntu
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
  - chmod a+r /etc/apt/keyrings/docker.asc
  - echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" > /etc/apt/sources.list.d/docker.list
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
  # Cho user đăng nhập chạy docker không cần sudo
  - usermod -aG docker azureuser
  - systemctl enable docker
  - systemctl start docker
  # Thư mục chứa app
  - mkdir -p /opt/recruitify && chown azureuser:azureuser /opt/recruitify

final_message: "Docker da cai xong sau %UPTIME giay. VM san sang deploy."
